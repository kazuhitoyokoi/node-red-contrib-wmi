name: Build Node-RED node
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: actions/setup-node@v3
    - run: |
        npm install -g node-red-nodegen node-red-dev
        node-red-nodegen flows.json --tgz
        cd node-red-contrib-*/
        #cat package.json | jq ".repository.type|=\"git\"|.repository.url|=\"https://github.com/$GITHUB_REPOSITORY.git\"" > tmp; mv tmp package.json
        cat package.json | jq ".bugs.url|=\"https://github.com/$GITHUB_REPOSITORY.git\"" > tmp; mv tmp package.json
        mv ../examples .
        node-red-dev validate
